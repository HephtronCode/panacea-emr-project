# =============================================
# === SECURITY WARNING - READ BEFORE USING! ===
# =============================================
# This example file is for local development ONLY.
# DO NOT copy this file to production as-is.
# In production, you MUST set strong, unique secrets
# via your hosting environment (e.g., cloud secrets manager).
#
# Specifically:
# - Replace JWT_SECRET with a long, random value (32+ chars)
# - Never use placeholders or example values in production
# - Rotate secrets regularly and store them securely

# --- Server Configuration ---
PORT=5000
NODE_ENV=development

# --- Database ---
# Format: mongodb+srv://<user>:<password>@cluster.mongodb.net/dbname
MONGO_URI=mongodb://localhost:27017/panacea_db

# --- Security ---
# Use a strong, long random string for production.
# PLACEHOLDER: replace below in real deployments.
# Examples of INVALID values that MUST NOT be used in production:
# - CHANGE_ME_IN_PRODUCTION
# - __REPLACE_ME__JWT_SECRET__
# - development_secret_key_12345
JWT_SECRET=CHANGE_ME_IN_PRODUCTION
JWT_EXPIRE=30d

# --- Frontend Integration ---
# Local Docker: http://localhost:5000/api
# Production Cloud: https://your-render-url.onrender.com/api
VITE_API_URL=http://localhost:5000/api